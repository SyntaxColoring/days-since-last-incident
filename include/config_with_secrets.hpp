#pragma once

namespace Config {
    const char* SSID = "ssid";
    const char* PASSWORD = "password";
    // To do: Double-check that these are the pedantically correct mDNS and DNS-SD terms to use.
    const char* HOSTNAME = "puns";
    const char* SERVICE_NAME = "Pun tracker._http";
}
